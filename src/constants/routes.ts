export enum Versions {
    v2 = '/api/v2'
}

export enum AppRoutes {
    home = '/',
    health = '/health',
    docs = '/docs'
}

export enum ACL {
    BRAND = 'BRAND',
    CUSTOMER = 'CUSTOMER',
    GUEST_OR_USER = 'GUEST_OR_USER'
}